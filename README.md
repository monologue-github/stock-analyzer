# stock-analyzer

基于**历史形态相似度匹配 + 多维融合 + 多算法消融选策略**的股票短线统计预测工具集。

**个人项目，欢迎找我交流（联系方式在网站底部）**

---

## v3.3 新增：全市场深历史 + 消融选策略 + 幽灵K线

### 1. 全A股 1000 交易日缓存（backfill_full.py）

```bash
python backfill_full.py          # 全量回填（断点续传，东财主源+腾讯兜底）
python backfill_full.py --limit 50   # 测试50只
```

- 每只 ≥1000 根前复权日K（约4.5年），并发+全局限流，进度百分比+ETA；
- 断点续传：已达标（≥950根且够新）的代码自动跳过；
- **免费源有IP配额**（东财批量~500只断连、腾讯单域~200请求后501）：脚本内置三域名轮换+501全局暂停自愈+断点续传，单晚推进数百只，**连续运行数晚覆盖全市场**；配置本地代理（ini [proxy]）可叠加配额；
- 深历史直接提升 L1/L2/L3 形态匹配的样本覆盖与统计预测准头。

### 2. 独立数据清洗（data_clean.py）

```bash
python data_clean.py          # 只扫描+生成《清洗报告_YYYYMMDD.md》
python data_clean.py --fix    # 扫描+修复（删异常bar/整只重拉/退市登记）
```

处理五类问题：**数据异常**（OHLC非法）、**除权残留**（涨跌幅越界→整只重拉前复权，ETF孤立跳变放行）、**停牌**（长缺口报告，不造数）、**退市**（记入 delisted 表）、**价格粘性**（冻结数据重拉）。

### 3. 多算法消融 → 三档策略弹窗（防过拟合）

- 策略缓存缺失/过期时，后台对该股**近1000交易日**跑一轮消融回测：L1形态 / MACD / KDJ / RSI / 布林带 / MA20-60趋势 / 多维评分×3风险档；
- **训练集(~75%)选型，验证集(~25%)只报告不参与选择**——弹窗里胜率/年化/回撤是样本外数据，杜绝"回测神话"；
- 弹窗列出**保守（回撤低胜率高）/ 稳健（收益回撤比最优）/ 激进（收益率高）**三档，各含**牛市评分**与**熊市评分**（上证 vs MA120 分段年化）；
- 选择后缓存 **5日**，期间分析直接复用：历史买卖点、止损/冷却参数全部按所选策略执行；到期自动重新消融。入口：工具 → 重选策略。

### 4. 幽灵K线：T+1 / T+5 / T+10 预测蜡烛

主图末尾追加三根**白色虚线边框**的预测K线（亮色主题自动转黑）：收=累计P50，高/低=累计区间（含均值回归衰减），悬停可看数值。策略决定买卖点与回测口径，幽灵K线始终由三级融合统计预测驱动。

### 5. numpy 加速

形态匹配热点路径（滑窗z-score距离矩阵）向量化，快约50倍；numpy 缺失时自动退回纯Python（香橙派可 `pip install numpy` 获得同等工作速度）。

---

## v2.0 算法原理

### 1. 形态匹配与样本池

**L1 自身历史**：将最近10日的归一化收益率序列作为形态切片，在标的自身600日全历史中用欧氏距离+量能距离加权匹配，取Top6最近样本。

**L2 同行业**：同行业股票的近期走势作为辅助样本池（权重0.3），捕捉板块联动效应。

**L3 同市值层**：相近市值股票的走势作为第三级样本（权重0.1），捕捉大小盘轮动规律。

三级样本按 **L1:0.6 / L2:0.3 / L3:0.1** 加权融合，用加权百分位数（P10/P25/P50/P75/P90）生成预测区间。

### 2. 量能状态与大盘环境筛选

- **量能分区**：按当日成交量/20日均量划分为「放量/平量/缩量」三个regime
- **大盘过滤**：剔除与当日上证涨跌差异>0.8pp的样本，保留市场环境相近的样本
- **板块过滤**：剔除与当日板块指数涨跌差异>1.2pp的样本
- **开盘缺口**：若上述筛选不足3个样本，改按开盘缺口±1%筛选

### 3. 筹码分布（Volume-at-Price Profile）

逐日演化计算筹码分布：
- 每根K线的成交量按高低价区间均匀摊分到价格格子
- 历史筹码按换手率衰减（换手率用量/中位量×2%启发式近似）
- 输出：平均成本、获利盘比例、90%筹码区间、支撑位/压力位（现价上下方最密集筹码峰）

### 4. 多维综合评分（买卖点核心）

每日对7个维度打分（+1/0/-1），方向切换时触发买卖信号：

| 维度 | 偏多(+1/+2) | 偏空(-1/-2) |
|------|-------------|-------------|
| MACD | DIF>DEA(+1), 金叉(+2) | DIF<DEA(-1), 死叉(-2) |
| KDJ | K>D(+1), 低位金叉(+2) | K<D(-1), 高位死叉(-2) |
| RSI6 | <30超卖(+1), 超卖回升(+2) | >70超买(-1), 超买回落(-2) |
| 量价 | 放量上涨(+1) | 放量下跌(-1) |
| MA20 | 价>MA20且MA20↑(+1) | 价<MA20且MA20↓(-1) |
| 筹码 | 贴近支撑(+1) | 贴近压力(-1) |
| 统计预测 | 上行概率≥55%(+1) | 上行概率≤45%(-1) |

**触发规则**：
- 得分≥2 且前一次信号为空头/无 → 生成 **BUY** 信号
- 得分≤-2 且前一次信号为多头/无 → 生成 **SELL** 信号
- 两次信号间隔≥5根K线（冷却期）
- 每天最多一个信号，买和卖自然配对

**综合评估**（侧栏展示）：同维度打分，输出合计分值和结论（偏多/略偏多/观望/略偏空/偏空）。

### 5. 缓存校验

- 加载本地缓存后，校验相邻日涨跌幅是否超出该股允许的涨跌幅范围（主板10%、创业板/科创板20%、北交所30%、主板ST新规10%/旧规5%）
- 留3个百分点容差吸收前复权除权分红造成的合法跳变
- 历史不足30根的新股跳过检查（上市初期无涨跌幅限制）
- 异常数据触发全量重新下载替换

---

## 文件说明

| 文件 | 说明 |
|---|---|
| `stock_gui.py` | **本地图形界面版**（推荐日常使用，唯一算法源） |
| `stock_predict.py` | 命令行精简版（由 build_cli.py 自动生成，勿手改） |
| `backfill_full.py` | 全A股1000交易日批量回填（断点续传） |
| `data_clean.py` | 独立数据清洗（停牌/除权/退市/异常，`--fix` 才改库） |
| `build_cli.py` | GUI→CLI 打包器（改完 GUI 必须重跑） |
| `stock_gui.ini` | 本地配置文件（自选池 / 上次查看 / 主题 / API Key），随使用自动生成 |
| `stock_web.py` | 网页版（部署在 Orange Pi，浏览器访问） |

**网页版入口**：<https://orangepizero2w-1.tail6a6cc8.ts.net/>（Tailscale Funnel 公网直连，手机/电脑浏览器均可打开）

## 功能（GUI 版）

- **K线主图**：MA5/10/20/30/60、筹码峰叠加（右侧横向剖面）、支撑/压力虚线、买卖点标记（B/S）、预测K线
- **副图**：成交量（含5日均量）、MACD / KDJ / RSI 可切换
- **十字光标**：三图联动，右侧价格轴 + 底部日期轴对齐显示
- **预测**：
  - 今日(T) 收盘/最高/最低 P10~P90 分位区间（锚定今开）
  - 次日(T+1) 预测K线
  - 相似历史参考日期列表
- **筹码参考**：平均成本、获利盘比例、90%筹码区间、支撑/压力位
- **综合评估**：多维打分买卖点参考（MA20/MACD/KDJ/RSI/量价/筹码/统计预测）
- **自选池**：左侧列表，双击分析，持久化到 ini
- **五大指数条**：上证 / 深成 / 创业板 / 科创50 / 北证50，30秒自动刷新
- **AI 分析**（DeepSeek）：整合技术面、量价、大盘板块环境、筹码分布与统计预测，输出操作建议；**支持多轮追问**（首轮携带完整数据上下文，追问只追加新问题，共享同一份数据，不重复拉取）
- **K线增量加载**：启动快速显示基础预测（走缓存秒开，快启动速度优先），随后**后台逐批加载同行业/同市值层样本池，预测K线边加载边更新**——样本越多，统计预测越准（大缓存提升准头）
- **策略消融弹窗**：首次分析/缓存到期时自动后台回测，三档策略（保守/稳健/激进）含牛熊评分供选择，5日缓存（见 v3.3 章节）
- **幽灵K线**：T+1/T+5/T+10 白色虚线预测蜡烛
- **缓存不自动清理**：本地缓存永远保留（可大），同市值层样本池同日共享、避免重复拉取浪费空间
- **个性化设置**：暗色/亮色主题、红涨绿跌/绿涨红跌

## 使用方法

```
python backfill_full.py        # 首次建议先跑：全A股1000交易日缓存（约2-3小时，断点续传）
python data_clean.py --fix     # 回填完成后清洗一次
python stock_gui.py            # 日常使用
输入代码（如 002241 / 600519 / sh600519），回车或点【分析预测】
滚轮：缩放K线    悬停：OHLC + 指标值    工具：重选策略/胜率计算/AI分析
```

## 数据与配置

- 行情源：东方财富（支持一次拉全量深历史）→ 腾讯行情接口（前复权日线）→ 网易163 → 新浪财经（多源容灾）
- 依赖：纯标准库即可运行；安装 numpy 可获得匹配加速（可选）
- 板块/指数：东方财富
- 配置文件 `stock_gui.ini`：

```ini
[watchlist]
codes = sz002241,sh600519
[ui]
last = sz000725
theme = dark        ; dark / light
updown = red_up     ; red_up / green_up
[deepseek]
api_key = sk-xxxx   ; 仅存本地
```

## 免责声明

本程序所有输出（包括但不限于 K 线、指标、形态相似度统计预测、筹码分布、综合评估、AI 分析）**仅为历史数据的技术统计与研究用途，不构成任何投资建议或收益承诺**。股市有风险，据此操作产生的盈亏与后果由使用者自行承担。请遵守所在地区法律法规，理性投资。
