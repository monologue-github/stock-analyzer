# stock-analyzer

基于**历史形态相似度匹配 + 多维融合 + 多算法消融选策略 + 自适应机器学习**的股票短线统计预测工具集。

**个人项目，欢迎找我交流（联系方式在网站底部）**

---

# v4.0 架构

v4.0 在 v3.3 的 L1/L2/L3 形态匹配核心之上，加入一套**个股自适应 ML 管线**（全部内嵌于单文件 `stock_gui.py`，CLI 由 `build_cli.py` 自动生成，仍是单文件）：

```
全A缓存日K ──> 13因子矩阵（逐日，仅用T日及以前数据）
                 │  L1形态up_prob/相似样本收益 + MA20/60乖离 + 5/10/20日收益
                 │  + RSI14 + ATR% + 20日波动 + 量变 + 大盘5日 + 行业5日
                 ▼
   Walk-Forward 时序验证（扩展窗，40日折，折间不重叠）
                 │  每折内（只用训练段）：
                 │   ① Horizon 自适应：训练段内层75/25切分，T+1/T+5/T+10 选优
                 │   ② Lasso：标准化因子 → 稀疏权重（全零则退回 IC×稳定性 加权）
                 │   ③ Logistic(L2)：p_up 方向概率
                 │   ④ LightGBM：非线性收益预测（浅树/强正则/先验超参）
                 │   ⑤ Quantile Regression：Q10/Q25/Q50/Q75/Q90 收益分布
                 ▼
   同一套模型输出（p_up / ml / Q10·Q50·Q75 / 自适应分数）
                 │
        ┌────────┼────────┐
        ▼        ▼        ▼
      保守      平衡      激进        ← 三档=同一模型的决策层参数，不分别训练
   （入场阈值/仓位/最大持仓数/退出置信度不同）
```

- **预测分布驱动退出**：初始止损 = Entry×(1+Q10)，棘轮式**只收紧不放宽**；Q75 为乐观止盈目标；p_up<阈值 信号退出。ATR止损/移动止盈（v3.3 风控）**保留为对照退出与极端行情防火墙**。
- **消融实验**：12 个变体（逐组件移除 + 关键组合），全部跑在**同一套 Walk-Forward 预测输出**上（决策层消融），只换入场/退出规则，公平比较。
- **统一回测口径**：同初始资金（100万）、同成本（滑点0.1%/佣金0.03%/印花税0.1%卖出）、同成交时点（信号日收盘）、涨停禁买/跌停顺延、停牌持仓顺延、期末强平。

# v3.3 → v4.0

| 组件 | v3.3 | v4.0 |
|---|---|---|
| 核心信号 | L1/L2/L3 形态匹配 + 多维评分 | 保留，并作为 ML 候选因子 |
| 验证方式 | 训练75%/验证25% 单次切分 | **Walk-Forward 扩展窗多折** |
| 预测目标 | 次日分布（分位区间） | **T+1/T+5/T+10 自适应 + 收益分布** |
| 模型 | 形态统计 + 规则评分 | + **Lasso / Logistic / LightGBM / Quantile** |
| 退出 | ATR止损/移动止盈 | + **Q10棘轮止损/Q75目标/p_up退出**（ATR保留为对照） |
| 风险分档 | 消融选策略三档（每档不同算法参数） | **同一套模型输出上的三档决策层** |
| 回测 | 每股事件回测，跨股中位 | + **组合级回测**（资金/成本/涨跌停/停牌统一） |
| 报告 | 消融弹窗 + v3.3 研究表 | + 本 README 统一报告 + `research/v4_report.json` |

**入口**：CLI `python stock_predict.py --v4 [--v4-limit N]`；GUI 工具菜单 →「v4.0 全A研究（三档风险+消融）」。产出落盘 `research/v4_report.json`（全指标）与 `research/v4_factors.json`（每股因子表）。历史报告归档在 `research/legacy/`。

---

# 全 A 股回测（v4.0 实证）

> 数据边界（如实记录）：本地缓存为**部分数据**——5556 只代码中有 778 只 A 股个股（沪深主板/创业板/科创板，剔除北交所、指数、ETF）具备 ≥400 根日K（中位约 404 根 ≈ 1.6 年）。全 A 回测在该缓存范围内执行，**非全市场完整覆盖**；补齐数据后可随时 `--v4` 复跑。

| 项目 | 数值 |
|---|---|
| 股票池 | 778 只（≥400根日K 的 A 股个股） |
| 有效完成 Walk-Forward | 778 只 |
| 组合回测（时间对齐） | 755 只（测试段结束距最新日≤45天；隐含幸存者偏差） |
| 测试窗口 | 每股最后 ~120 个交易日（约 6 个月），折大小 40 日 |
| 模型统计样本 | 95,080 股·日（每股·日含 3 个 Horizon 预测） |
| 成本 | 滑点 0.1%/边 + 佣金 0.03% + 印花税 0.1%（卖出） |
| 成交时点 | 信号日收盘；涨停禁买、跌停顺延卖、停牌顺延、期末强平 |

## 三档风险（同一套 v4 模型输出，仅决策层不同）

| 策略 | 总收益 | 年化 | 最大回撤 | Calmar | Sharpe | 波动率 | 胜率 | 盈亏比 | 交易数 | 平均持仓(日) | 最大连亏 |
|---|---|---|---|---|---|---|---|---|---|---|---|
| **Full v4 · 保守** | -1.7% | -0.9% | -24.2% | -0.04 | -0.05 | 9.9% | 30% | 0.97 | 383 | 3.0 | 20 |
| **Full v4 · 平衡** | -6.4% | -3.5% | -21.1% | -0.16 | -0.26 | 11.8% | 29% | 0.92 | 243 | 3.4 | 18 |
| **Full v4 · 激进** | -11.2% | -6.1% | -25.2% | -0.24 | -0.34 | 16.0% | 35% | 0.88 | 165 | 3.5 | 15 |
| Adaptive · 保守 | +44.0% | +21.4% | -32.3% | 0.66 | 0.93 | 25.5% | 22% | 3.26 | 32 | 43.6 | 24 |
| Adaptive · 平衡 | +81.7% | +37.3% | -38.3% | 0.97 | 1.23 | 31.2% | 30% | 7.09 | 10 | 88.4 | 7 |
| Adaptive · 激进 | +27.4% | +13.7% | -30.6% | 0.45 | 0.57 | 34.5% | 40% | 4.27 | 5 | 105.6 | 3 |
| v3.3 Baseline · 保守 | -18.3% | -10.2% | -32.9% | -0.31 | -0.49 | 19.5% | 21% | 0.72 | 154 | 8.9 | 31 |
| v3.3 Baseline · 稳健 | **+15.2%** | **+7.8%** | -24.9% | **0.31** | **0.46** | 22.6% | 27% | 1.24 | 82 | 10.4 | 19 |
| v3.3 Baseline · 激进 | -2.0% | -1.0% | -29.7% | -0.04 | 0.08 | 24.8% | 24% | 0.96 | 51 | 11.0 | 10 |

> 三档参数：保守 p_up≥0.62 / 收益预测≥1.2% / 自适应≥1.0σ / 单仓12% / 最多10仓；平衡 0.55 / 0.6% / 0.7σ / 20% / 6仓；激进 0.52 / 0% / 0.4σ / 33% / 4仓。

# Baseline vs v4.0

**直接结论：v4.0 Full 没有战胜 v3.3 Baseline。**

| 口径 | v3.3 Baseline（稳健） | v4.0 Full（平衡，最优档） |
|---|---|---|
| 年化 | **+7.8%** | -3.5% |
| 最大回撤 | -24.9% | **-21.1%**（略浅） |
| Calmar | **0.31** | -0.16 |
| Sharpe | **0.46** | -0.26 |
| 交易数 | 82 | 243 |

信号质量（IC）上 v4 的 ML 模型确实更强（见下表），但在 ~6 个月测试窗 + 真实成本 + 短持仓（3~4 天，单边成本 0.13%~0.23%）条件下，不足以转化为组合正收益。v4 的价值目前体现在**回撤略浅**与**更丰富的分布/概率输出**，不是收益。

# Horizon 实验

LightGBM 预测 vs 对应 Horizon 真实收益（95,080 样本，严格样本外）：

| Horizon | IC | IC_IR(跨股) | MAE | 方向命中 | 样本 |
|---|---|---|---|---|---|
| T+1 | +0.003 | +0.05 | 0.030 | 49.9% | 95,080 |
| T+5 | +0.018 | +0.18 | 0.074 | 50.5% | 95,080 |
| T+10 | **+0.044** | **+0.25** | 0.111 | **51.3%** | 95,080 |

- 预测力随 Horizon 变长单调增强（短线上噪声主导，与 v3.3 "RSI 反向、动量存在"的结论一致）。
- 个股 Horizon 自适应（训练段内层切分选优）选择分布：T+1 27%、T+5 25%、T+10 48%。
- 消融显示把自适应 Horizon 退化为固定 T+5 几乎不改变结果（见消融表）——**Horizon 自适应在本数据上贡献≈0**。

# ML 模型比较

vs 当日所选 Horizon 真实收益（pooled，样本外）：

| 模型 | IC | MAE | 方向命中 | 样本 |
|---|---|---|---|---|
| Quantile Q50 | **+0.039** | **0.074** | 51.2% | 95,080 |
| Logistic p_up | +0.036 | - | 51.2% | 95,080 |
| LightGBM | +0.032 | 0.080 | 50.8% | 95,080 |
| Lasso 自适应得分 | +0.017 | - | 50.6% | 95,080 |
| L1 形态 up_prob | +0.003 | - | 51.2% | 95,080 |
| L1 相似样本收益 | +0.004 | 0.071 | 49.8% | 95,080 |
| v3.3 多维评分状态 | +0.005 | - | 50.0% | 6,451 |

- **LightGBM 有用但过拟合标记为真**：训练段内 IC 中位 0.657 vs 样本外 pooled IC 0.032（⚠️）。原因明确：每股训练样本仅 ~200-300 根。已采取的抑制措施：max_depth=4、num_leaves=15、min_child_samples=40、reg_lambda=1.0、子采样 0.8、超参先验固定不按 Test 调整。样本量上来之前，其样本外贡献应按"小幅"理解。
- 线性家族（Q50/Logistic）样本外 IC 反而略高于 LightGBM —— 与过拟合标记一致。
- v3.3 的 L1 形态/多维评分 IC 接近 0（日频），但 v3.3 基线组合收益更好——再次说明**信号 IC ≠ 组合收益**（退出结构、持仓周期、成本都参与定价）。

# 消融实验

全部变体跑在同一套 Walk-Forward 预测输出上（决策层消融，仅改入场/退出规则；平衡档；IC=该变体入场分数 pooled IC）：

| 变体 | 年化 | 最大回撤 | Calmar | Sharpe | 胜率 | 交易数 | 入场IC |
|---|---|---|---|---|---|---|---|
| **Full v4** | -3.5% | -21.1% | -0.16 | -0.26 | 29% | 243 | +0.032 |
| Full - Adaptive | -9.7% | -30.4% | -0.32 | -0.75 | 36% | 269 | +0.032 |
| Full - Horizon | **-3.1%** | -23.5% | -0.13 | -0.26 | 32% | 231 | +0.018 |
| Full - Logistic | -5.4% | -23.3% | -0.23 | -0.40 | 32% | 200 | +0.032 |
| Full - LightGBM | -13.9% | -29.5% | -0.47 | -1.36 | 29% | 211 | +0.027 |
| Full - Quantile | +34.5% | -38.6% | 0.89 | 1.21 | 38% | 13 | +0.032 |
| Full - DistributionExit | +34.5% | -38.6% | 0.89 | 1.21 | 38% | 13 | +0.032 |
| Adaptive + Horizon | -6.8% | -18.8% | -0.36 | -0.55 | 31% | 181 | +0.027 |
| Adaptive + LightGBM | +42.7% | -36.6% | 1.17 | 1.35 | 50% | 8 | +0.018 |
| Logistic + LightGBM | +35.6% | -35.1% | 1.01 | 1.24 | 21% | 19 | +0.032 |
| LightGBM + Quantile | -6.2% | -36.2% | -0.17 | -0.40 | 43% | 243 | +0.032 |
| Adaptive + Horizon + LightGBM | +34.9% | -36.8% | 0.95 | 1.22 | 29% | 21 | +0.032 |

**哪个组件真的贡献了性能（平衡档）：**

- **LightGBM：有正贡献**——移除后年化 -13.9%（vs Full -3.5%）、Sharpe -1.36。但注意其过拟合标记为真，贡献可能不稳定。
- **Adaptive（Lasso 自适应过滤）：有正贡献**——移除后年化 -9.7%、回撤 -30.4%。
- **Logistic：有小幅正贡献**——移除后 -5.4%。
- **Horizon 自适应：贡献≈0**——移除后 -3.1% 甚至略优。
- **Quantile/分布退出：无法判定**——"Full - Quantile"与"Full - DistributionExit"结果完全相同（13 笔交易），ATR 对照退出下交易数骤减，小样本不具统计意义。
- 正年化变体（+34%~+43%）全部是 **8~21 笔交易的小样本**，由个别持仓主导，**不作为有效证据**。

# 预测分布

Quantile Regression（Q10~Q90，线性、训练段拟合、排序修复交叉）：

| 指标 | 数值 | 说明 |
|---|---|---|
| Pinball Loss | P10 0.018 / P25 0.029 / P50 0.037 / P75 0.033 / P90 0.022 | 收益单位（绝对量级） |
| 区间覆盖率 [Q10,Q90] | **66.4%**（名义 80%） | **区间系统性偏窄**，与 v3.3 分位区间偏窄问题同向 |
| Quantile crossing（原始） | 0.63% 相邻对 | 排序投影修复后为 0（修复方式保守、如实记录） |

分布退出（Q10 棘轮止损 + Q75 目标）在本数据上交易数太少，无法证明优于 ATR 对照退出。

# 风险分析

- **成本敏感**：Full v4 平均持仓仅 3~4 天，单边成本 0.13%~0.23%，年化被成本吃掉 ~4-8 个百分点；拉长持仓或降频是首要改进方向。
- **集中度**：激进档单仓 33%、最多 4 仓，单票黑天鹅冲击 ≈ -33%×跌停幅度；保守档 10 仓×12% 分散但收益摊薄。
- **市场段单一**：组合测试窗仅 ~6 个月（2026-02 ~ 2026-08 缓存尾段），未覆盖完整牛熊；Calmar/Sharpe 不具备长期代表性。
- **幸存者偏差**：组合回测要求测试段结束接近最新日（755/778），退市股只参与 IC 统计。
- **小样本陷阱**：本报告所有正年化结果的交易数都很少（≤32 笔），不做显著性检验前一律视为噪声。

# 数据与回测方法

- 数据源：腾讯/东财/网易/新浪多源前复权日K，本地 SQLite 缓存（`stock_cache.db`），回填/清洗见下文 v3.3 章节。
- 因子（13个，全部 T 日及以前）：L1形态up_prob、L1相似样本收益（逐日自身历史匹配，与 v3.3 `_sig_l1_pattern` 同口径）；MA20/60 乖离；5/10/20 日收益；RSI14；ATR14/Close；20日波动率；量变(5/20)；大盘5日收益；行业5日收益（市场/行业等权日收益由缓存一次性预计算）。
- 标签：`Close[T+H]/Close[T]-1`，H∈{1,5,10}，仅作训练目标。
- Walk-Forward：扩展窗，折 40 交易日，首折前训练 ≥180 日，折间不重叠；每股 3~4 折。
- 训练段内完成：标准化（Scaler 只 fit 训练段）、Lasso 筛选（α=0.005，全零退回 IC×稳定性加权）、Horizon 选择（内层 75/25 切分）、全部模型拟合。
- LightGBM 超参（先验固定）：`num_leaves=15, max_depth=4, lr=0.05, n_estimators=200, min_child_samples=40, subsample=0.8, colsample=0.8, reg_lambda=1.0`。
- 组合模拟：现金+持仓逐日 mark-to-market；买入=收盘×(1+滑点)(1+佣金)，卖出=成交价×(1-滑点)(1-佣金-印花税)；一手 100 股整数化；期末强平。

# 数据泄漏检查

✅ 特征仅用 T 日及以前数据（形态匹配样本窗口结束于 T-20 之前）
✅ 标签仅作训练目标，不进入特征
✅ StandardScaler 仅在训练段 fit
✅ 因子筛选（Lasso/IC）仅用训练段
✅ Horizon 选择仅用训练段内层切分
✅ LightGBM 超参先验固定，未按 Test 调整
✅ Walk-Forward 每折仅用折前数据，折间不重叠
✅ 三档风险为同一模型输出上的决策层参数，未分别训练
✅ 组合回测统一资金/成本/成交时点/涨跌停/停牌规则
⚠️ LightGBM Train IC(0.657) >> Test IC(0.032)，已如实标记为疑似过拟合
⚠️ 本 README 撰写过程中查阅了 Test 结果（用于报告而非调参）；模型/阈值参数未因 Test 结果修改

# 当前结论

1. **v4.0 Full 没有战胜 v3.3 Baseline**（年化 -3.5% vs +7.8%，Calmar -0.16 vs 0.31）；唯一占优项是最大回撤（-21.1% vs -24.9%）。
2. **信号质量确实提升**：ML 家族样本外 IC（0.032~0.039）显著高于 v3.3 的 L1 形态（0.003）与多维评分（0.005），但不足以覆盖成本与执行损耗。
3. **消融**：LightGBM > Adaptive 过滤 > Logistic > Horizon≈0；Quantile/分布退出无法判定（交易太少）。
4. **LightGBM 过拟合标记为真**（Train 0.657 vs Test 0.032），当前样本量（每股 ~200-300 训练样本）不足以支撑非线性模型完全发挥。
5. **Quantile 区间偏窄**（覆盖 66% vs 名义 80%），Q10 止损线因此偏紧，可能是分布退出交易过少的原因之一。
6. 正确用法：把 v4 输出当作**低频过滤与风险底线工具**（T+10 IC 最高、Q10 作为风险底线），而不是日频交易信号源。

# 已知局限

- 本地缓存为部分数据（778/5556 只具备 ≥400 根日K），补齐后结论可能变化；
- 组合测试窗仅 ~6 个月、单一段落，无分年度稳定性；
- 训练样本量小 → LightGBM 过拟合、Lasso 全零回退频繁触发；
- 涨跌停/停牌为简化规则（涨停禁买、跌停顺延），未建模一字板无法成交的极端情形；
- 未建模大额资金冲击成本；
- 消融为决策层消融（同一套预测输出），未重训模型侧变体；
- Quantile 为线性模型，未覆盖非线性分布形状。

---

# v3.x 算法原理（保留，构成 v4 因子与基线）

## 1. 形态匹配与样本池

**L1 自身历史**：将最近20日的归一化收益率序列作为形态切片，在标的自身全历史中用欧氏距离+量能距离加权匹配，取Top-K样本。
**L2 同行业** / **L3 同市值层**：辅助样本池（可关），捕捉板块联动与大小盘轮动。
三级样本加权融合，用加权百分位数（P10~P90）生成预测区间（校准系数 1.3/1.4）。

## 2. 量能状态与大盘/板块环境筛选

量能分区（放量/平量/缩量）、大盘过滤（涨跌差>0.8pp剔除）、板块过滤（>1.2pp剔除）、开盘缺口兜底。

## 3. 筹码分布（Volume-at-Price Profile）

逐日演化：成交量按高低价摊分到价格格子、按换手率衰减；输出平均成本、获利盘、90%筹码区间、支撑/压力位。

## 4. 多维综合评分（v3.3 买卖点核心）

MACD/KDJ/RSI6/量价/MA20/筹码/统计预测/ADX 八维打分，≥+2 触发 BUY、≤-2 触发 SELL，冷却 5 根K线。v3.3 消融弹窗：训练75%选型/验证25%只报告，三档（保守/稳健/激进）牛熊分段评分，缓存5日。

## 5. 缓存校验

相邻日涨跌幅超限校验（主板10%、创业/科创20%、北交所30%，留3pp容差），异常触发重拉。

---

# 文件说明

| 文件 | 说明 |
|---|---|
| `stock_gui.py` | **本地图形界面版（推荐日常使用，唯一算法源，单文件）**——v4.0 引擎内嵌于此 |
| `stock_predict.py` | 命令行版（由 build_cli.py 自动生成勿手改；单文件独立运行；含 `--backfill/--clean/--research/--v4/--push`） |
| `build_cli.py` | GUI→CLI 打包器（改完 GUI 必须重跑） |
| `data_clean.py` | 独立数据清洗（与主程序同规则，`--fix` 才改库） |
| `backfill_full.py` | 旧版独立回填脚本（已集成进 GUI/CLI，保留备用） |
| `research/` | v4.0 实验产物：`v4_report.json`（全指标）、`v4_factors.json`（每股因子表）、`legacy/`（历史报告归档） |
| `stock_gui.ini` | 本地配置（自选池/主题/API Key），随使用自动生成 |
| `stock_web.py` / `pi_stock_web.py` | 网页版（部署在 Orange Pi，浏览器访问） |

# 使用方法

```
python stock_predict.py --backfill   # 首次建议先跑：全A股1000日缓存（断点续传）
python stock_predict.py --clean      # 回填后清洗一次
python stock_predict.py --v4         # v4.0 全A研究：三档风险回测+消融（写入 research/）
python stock_predict.py --v4 --v4-limit 50   # 先小池试跑
python stock_gui.py                  # 日常使用
输入代码（如 002241 / 600519 / sh600519），回车或点【分析预测】
工具菜单 → v4.0 全A研究（三档风险+消融）：后台运行，完成后弹摘要
```

依赖：纯标准库可运行；`numpy` 形态匹配加速；**v4.0 研究需要 `numpy + scikit-learn + lightgbm`**（缺失时如实报告依赖问题，不偷偷降级模型）。

# 数据与配置

- 行情源：东方财富 → 腾讯行情（前复权日线）→ 网易163 → 新浪财经（多源容灾）
- 板块/指数：东方财富
- 配置文件 `stock_gui.ini`：

```ini
[watchlist]
codes = sz002241,sh600519
[ui]
last = sz000725
theme = dark        ; dark / light
updown = red_up     ; red_up / green_up
[deepseek]
api_key = sk-xxxx   ; 仅存本地
```

# 免责声明

本程序所有输出（包括但不限于 K 线、指标、形态相似度统计预测、筹码分布、综合评估、AI 分析、v4.0 ML 预测与回测结果）**仅为历史数据的技术统计与研究用途，不构成任何投资建议或收益承诺**。股市有风险，据此操作产生的盈亏与后果由使用者自行承担。请遵守所在地区法律法规，理性投资。
