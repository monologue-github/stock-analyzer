# 架构说明 · 股票形态相似度预测系统

> 版本 v3.3　更新于 2026-09-05
> 配套《REPORT_v3.2.md》（重构修复明细）、《报告_测试_v3.3.md》（本版测试）。本文只讲两件事：**这套东西的架构**和**每个环节用的什么算法**。不含数据结果。

---

## 一、总体架构

```
 Windows 开发机                                Orangepi (192.168.3.28)
 ┌─────────────────────────┐                  ┌──────────────────────────────┐
 │ stock_gui.py            │  python          │ ~/stock_predict.py           │
 │  = 唯一算法源（单体文件）│ ──build_cli.py──▶│  CLI 单文件版（含缓存层）     │
 │  缓存层 + 算法层 + GUI   │   抽取拼装       │   ├ 本地分析 / 回测           │
 ├─────────────────────────┤                  │   └ --push: scp JSON 报告 ──┐ │
 │ stock_predict.py        │                  │                             ▼ │
 │  （生成物，勿手改）      │                  │ ~/ai-quant/memory/inbox/    │
 ├─────────────────────────┤                  │  （ai-quant 收件箱）         │
 │ stock_web.py            │                  ├──────────────────────────────┤
 │ pi_stock_web.py         │ ───部署─────────▶│ ~/webapp/stock_web.py        │
 │  （Web 弱算法版两个变体）│                  │  systemd: ai-stock-web :8010 │
 └─────────────────────────┘                  │  完整版 / 轻量版lite /text    │
                                              └──────────────────────────────┘
```

### 组件职责

| 组件 | 角色 | 说明 |
|---|---|---|
| `stock_gui.py` | **唯一算法源** | Tkinter GUI。内嵌缓存层在算法前面，`build_cli.py` 按“锚点注释”抽取缓存块（`# ===== 内嵌缓存层`）和算法块（`QT_URL =` 到 `def slice_view`）拼出 CLI |
| `build_cli.py` | 打包器 | 保证 GUI/CLI 同一套算法；改完 GUI 必须重新运行它。禁止往算法块里放 tkinter 代码（有 assert 校验） |
| `stock_predict.py` | CLI 生成物 | 纯标准库单文件，可在无 GUI 的机器（如香橙派）上跑：分析、回测统计、`--push` 推报告到 ai-quant 收件箱、`--refresh-cache` 刷新代码表 |
| `~/ai-quant/` | 下游量化系统 | 从 `memory/inbox/` 收 JSON 分析报告（每次推送一个 `stock_<code>_<date>.json`），旧文件进 archive |
| `~/webapp/stock_web.py` | 手机网站 | 标准库 http.server，端口 8010：完整版 `/`（前端 JS 全算）、无JS文本版 `/text`（服务端算）、极简版 `/lite` |
| `stock_web.py` / `pi_stock_web.py`（本地） | Web 源码两个变体 | `stock_web.py` 功能更全（多指标/触摸手势等），`pi_stock_web.py` 为其精简快照；**线上以 Pi 上被部署的那份为准** |

### 关键架构约束（为什么这么设计）

1. **单文件算法源**：算法永远只改 `stock_gui.py` 一处，GUI/CLI 天然同步；代价是不能拆模块，靠内部清晰的分层（缓存层/数据获取/指标计算/分析/GUI 五大区块注释分隔）弥补。
2. **生成物不可手改**：`stock_predict.py` 头部注明由脚本生成。
3. **弱依赖设计**：Web 端不共享 GUI 代码（香橙派场景要免安装、抗手机低算力），因此它是"同一思想的弱化独立实现"，跟主算法是近亲不是引用关系。

---

## 二、数据层（缓存 & 容灾）

### 2.1 多源日K容灾
优先级链路：**腾讯 → 东方财富(4host轮询) → 网易163(CSV) → 新浪(JSON)**。
- 对每个源维护**熔断器**状态 `[连续失败数, 熔断截止时间]`：
  - 限流类错误（HTTP 429/502/503/504）连续 ≥2 次 → 触发熔断，冷却 `60s × 2^(n-2)`，上限 600s；
  - 所有源都在冷却时的极端情况 → 选最早解禁的源**半开探测**，成功即重置；
- `_http_get` 全局限速 0.16s + **指数退避带 ±25% 抖动**；限流错误只重试 1 次就切下一源。
- 所有出网请求（行情/K线/板块）统一走该通道并向熔断器上报。

### 2.2 SQLite 缓存（stock_cache.db）
- 表：`daily_bars(code,date主键)` / `stocks(全市场代码表)` / `meta` / `failed(负缓存)`；
- WAL 模式 + NORMAL 同步；所有连接走 `db_conn()` 上下文管理器（自动提交/回滚/关闭）；
- **增量回填**：本地最新一根 < 上个工作日才拉远端，`INSERT OR REPLACE` 合并；
- **负缓存**：从未拉取成功的代码记录失败原因，1 小时内不再重试；
- **脏数据校验**：入库前后做两道检查——① 单根K线合法性（负价/高低颠倒/影线超实体1.5倍剔除）；② 相邻日涨跌幅超出「当日允许涨跌停幅+3pp」判异常（主板10%、创业/科创20%按注册制时间分段、北证30%、ST旧规5%）。ETF 因除权折算会有合法大跳变，采用"孤立跳变放行、连续跳变才判错"策略。

### 2.3 分层池构建
全A代码表从东财分页拉取（每7天刷新一次），按市值三分位切成 **大盘/中盘/小盘** 三层，存 stocks 表（industry/tier 建索引）。

### 2.4 全市场深历史回填（backfill_full.py，v3.3）
- 目标：全A每只 ≥1000 交易日前复权日K（东财一次请求 `lmt=1100`，腾讯兜底）；
- 断点续传：本地 ≥950 根且最新bar够新的代码直接跳过；并发 8 线程 + 全局限流，进度百分比 + ETA；
- 顺手刷新全市场代码表（缺代码表时自动触发），回填完 GUI 的 L2/L3 池立即可用深历史。

### 2.5 独立数据清洗（data_clean.py，v3.3）
直接操作 stock_cache.db（不依赖主程序），`--fix` 才改库，默认只出报告（`清洗报告_YYYYMMDD.md`）：
1. **结构异常**：OHLC缺失/非正/高低颠倒/影线越界 → 删除该bar；
2. **除权残留**：相邻日涨跌幅超「当日涨跌停+3pp」（前复权序列不该出现）→ 整只代码重新拉取前复权全量替换；ETF 孤立跳变放行（分红/折算合法）；
3. **停牌**：上市区间内 >20 天日历缺口/零成交 → 报告（缺数据是事实，不造数）；
4. **退市**：最后bar距今 >180 天 → 记入 `delisted` 表；
5. **价格粘性**：连续 ≥20 日收盘价不变（坏源冻结）→ 视同异常整只重拉。

---

## 三、算法层

### 3.1 核心：形态相似度匹配
- 特征：最近 `W=10` 日**对数收益率**序列做 z-score 标准化；
- 距离 = `Σ(当前窗口 − 历史窗口)²` 的平方根（欧氏距离），再加三个环境惩罚项：
  - 量能：`|ln(今日量比/历史量比)|`（量比=近5日均量/前15日均量），缺失记 0.30；
  - 大盘：当日上证涨跌百分点差的线性惩罚，上限 1.5；
  - 板块：所属行业板块指数涨跌点差惩罚，上限 1.2；
- 取 Top-K=10 个最相似的历史"窗口末日" T 作为样本；
- ETF 的收益率序列先做 **MAD(中位数绝对偏差) 截断平滑**（±5×MAD），消除除权跳变的假形态。

### 3.2 三级样本池融合预测（L1/L2/L3）
| 层 | 含义 | 取样方式 | 权重 |
|---|---|---|---|
| L1 | 目标股自身历史 | 上面的 Top-K 匹配 | 0.6 |
| L2 | 同行业 | 行业内市值对数距离最近的 N=50 只（走各自历史匹配） | 0.3 |
| L3 | 同市值层 | 与目标同层的股票池随机抽样 N=100（当天共享同一份抽样，排除 L2 已覆盖行业） | 0.1 |

- 预测输出：T日（盘中实时锚定今开）与 T+1 的 **P10/P25/P50/P75/P90 收开高低区间**——每档都是三级样本对应字段（次日收/开/高/低相对基准的涨幅）的**加权分位数**；
- **上行概率** = 三级各自样本"次日收涨占比"按权重加权；
- 盘中修正：预测上下轨强制包含已实现的最高/最低价；
- **多日预测 T+1..T+10**：逐日用样本 n{d}_cl 分布出 P50 累计曲线，叠加**均值回归衰减** `decay=max(0.6, 1−0.04(d−1))`，天数越远越向零回归，抑制误差累积。

### 3.3 信号引擎（多维打分 + 方向交替）
对每个交易日 i 计算 6 维得分：
| 维度 | 加分 | 减分 |
|---|---|---|
| MACD | 金叉 +2；DIF>DEA +1 | 死叉 −2；DIF<DEA −1 |
| KDJ | 低位金叉(K<45) +2；K>D +1 | 高位死叉(K>65) −2；K<D −1 |
| RSI6 | 超卖回升(<20→上穿20) +2；<30 +1 | 超买回落(>80→跌破80) −2；>70 −1 |
| 量价 | 放量上涨 +1 | 放量下跌 −1 |
| MA20 | 价站上且MA20向上 +1 | 价跌破且MA20向下 −1 |
| 筹码 | 贴近支撑峰 +1 | 顶到压力峰 −1 |

触发规则（全部只用当日及以前的数据，**无前视**）：
- 得分 ≥2 且上次方向非多头 → BUY；≤−2 且上次方向非空头 → SELL；
- **方向必须交替**，相邻信号冷却 5 个交易日；
- 弱势日过滤：当日大盘 <−1.5% 或板块 <−2% 时 BUY 需要 ≥3 分（用当日值查表，绝不拿"今天的大盘"回溯历史信号）。

### 3.4 波段适合度路由
打分（0-100）：波动率适中(25) + 趋势斜率/乖离(30) + 量能活跃(25) + 站上MA60(20)。≥60 用上面的多维信号；否则整段换成 **MA20/MA60 趋势跟踪**（金叉买入死叉卖出 + 当日大盘 >−1.2% 过滤），少而稳。

### 3.5 筹码分布
- 每日成交量按换手衰减历史筹码（换手率≈量/中位量×2%，限幅 0.2%~0.002），当日筹码在该日[low,high]区间均匀摊分到价格桶；
- 支撑/压力取现价上/下方**最密集局部峰**（避免噪声尖刺）；获利盘=P(成本≤现价)。

### 3.6 回测引擎（backtest_signals）
信号日收盘价成交：BUY 开仓、SELL 平仓；持仓期间挂**双止损**——`ATR14×2` 固定止损与 `持仓最高价×0.92` 移动止盈取高者，日内 low 触及即离场。输出交易数/胜率/区间收益/年化/最大回撤/盈亏比。

### 3.7 AI 分析（可选）
把双端结构化摘要（近10日OHLCV、MA/MACD/KDJ/RSI 最新值、大盘/板块环境、统计预测区间、支撑压力、多维评估结论）拼成 prompt 发 DeepSeek 多轮对话接口，模型默认 `deepseek-v4-pro`（ini 可配），支持共享上下文追问。

### 3.8 策略消融引擎（v3.3，防过拟合选型）
每次分析（策略缓存缺失/过期时）对该股**近1000交易日**做一轮多算法消融回测：

- **候选算法**：L1形态上行概率（逐步匹配、只用当日以前窗口）/ MACD金叉死叉 / KDJ金叉死叉 / RSI超买超卖 / 布林带回归 / MA20-60趋势 / 多维评分×3风险档（买入阈值+冷却+止损宽度联动）；
- **防过拟合**：前 ~75% 为**训练集**（选型只看它），后 ~25% 为**验证集**（只报告不参与选择）。每档风险等级在训练集上按各自目标选优——保守=最小回撤、激进=最大年化、稳健=最大收益回撤比；
- **牛熊评分**：以上证收盘 vs MA120 为牛熊分界，分别计算策略权益曲线在牛市段/熊市段的年化收益；
- **策略缓存**：用户三选一后存 meta 表（`strategy:<code>`），**5日过期**，期内分析直接复用（历史买卖点、止损参数全部按所选策略执行）；指标型策略选中后波段路由不再覆盖用户选择；
- 前视零容忍：所有信号只用 T 日及以前数据、信号日收盘成交、止损用其后日内 low/high。

### 3.9 幽灵K线（v3.3）
T+1（预测K线）/T+5/T+10 三根**白色虚线边框**蜡烛（亮色主题自动转黑）追加在主图末尾：开=锚点/前预测日收盘，收=多日预测累计 P50，高/低=累计 P75/P25（均值回归衰减后），并做 open/close 包夹修正。悬停显示"预测T+5(累计)"等标签。

---

## 四、Web 端算法（弱化独立实现）

Pi 网站 `/`（浏览器内 JS 自算）与 `/text`（服务端 analyze_server）：
- 匹配：同样是 10 日 logret z-score + 欧氏距离 Top-K，但只有 L1 自身历史（无三级池）、无熔断；
- 预测：同样的加权分位区间法，锚今开；
- 信号：MACD/KDJ/RSI 三维事件打点，**已修复"只会买"问题**——原实现三个指标各自独立出点、且 BUY 条件天然宽松（金叉只需 K<45，而死叉需 K>65），导致图上几乎全是买点；现改为与 GUI 一致的「方向必须交替 + 冷却 5 根K线」后置过滤器（三处实现均已打补丁并在 Pi 上验证生效）。
- `/lite` 页面为老设备设计的 ES5 降级版，同样应用了交替过滤。

---

## 五、近期变更索引

- v3.3 全市场1000交易日缓存（backfill_full）+ 独立数据清洗（data_clean）+ 多算法消融选策略（训练/验证切分、5日缓存）+ 幽灵K线T+1/T+5/T+10 + numpy向量化加速（匹配热点路径，缺失自动退回纯Python）：见《报告_测试_v3.3.md》
- v3.2 重构修复（线程锁/连接管理/日志/前视偏差/熔断统一/调参集中/自适应布局/future回调）：见《REPORT_v3.2.md》
- v3.2.1 网站信号买卖交替修复 + CLI 部署香橙派 + SSH 免密推送链路（本文所载架构即现状）

*免责声明：本系统所有输出仅为历史数据的统计推断与研究用途，不构成任何投资建议。*
