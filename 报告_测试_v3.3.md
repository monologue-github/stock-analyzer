# 测试报告 v3.3 · 全市场深历史缓存 + 消融选策略 + 幽灵K线

> 日期：2026-09-05 深夜版　测试环境：Windows 11 / Python 3.11.8 / numpy 2.4.6（可缺省）
> 测试人：獨白（AI 辅助）　配套：《ARCHITECTURE.md》v3.3、《README.md》v3.3

---

## 一、本次交付清单

| 模块 | 文件 | 说明 |
|---|---|---|
| 全市场深历史回填 | `backfill_full.py`（新增） | 全A股每只目标≥1000交易日前复权日K；断点续传/多域名轮换/限流自愈/并发+ETA |
| 独立数据清洗 | `data_clean.py`（新增） | 结构异常/除权残留/停牌/退市/价格粘性 五类；默认只报告，`--fix` 才改库，输出《清洗报告_YYYYMMDD.md》 |
| 多算法消融选策略 | `stock_gui.py` 策略消融引擎（新增~500行） | L1形态/MACD/KDJ/RSI/布林/MA趋势/多维评分×3风险档，近1000交易日回测，训练/验证切分防过拟合 |
| 策略缓存与弹窗 | `stock_gui.py` GUI | 三档策略弹窗（保守/稳健/激进，含牛市/熊市评分），选择缓存5日，历史买卖点按所选策略执行 |
| 幽灵K线 | `stock_gui.py` 绘图 | T+1/T+5/T+10 白色虚线边框预测蜡烛（亮色主题自动转黑），随三级融合预测更新 |
| numpy 加速 | `stock_gui.py`/`build_cli.py` | 滑窗z-score距离矩阵向量化（实测约50倍）；numpy缺失自动退回纯Python |
| CLI 同步 | `stock_predict.py`（重新生成） | 含缓存层+消融引擎+策略缓存（CLI无弹窗，默认多维·稳健） |

## 二、测试结论（全部通过）

| # | 测试项 | 结果 | 证据/数据 |
|---|---|---|---|
| 1 | 回填脚本端到端 | ✅ | 首测5只2s成功；单只拉取1600根（2×800翻页去重）；断点续传跳过达标代码 |
| 2 | 深历史数据规模 | ✅ | 截至测试时点：~140万根日K / 396只已达≥950根（限流下持续增长，见第五节说明） |
| 3 | 消融引擎（1000根） | ✅ | 1000根×8算法族回测 + 训练/验证切分 **0.2~2s**（numpy路径）；输出三档候选含验证期胜率/年化/回撤/牛熊评分 |
| 4 | 防过拟合设计 | ✅ | 选型只用训练集（前~75%）；弹窗展示验证集（后~25%）样本外指标；实测某股训练最优策略验证期胜率16.7%（诚实展示过拟合gap） |
| 5 | 牛熊评分 | ✅ | 上证 vs MA120 分段年化：实测 牛+19.8% / 熊-23.0%（MACD·激进档） |
| 6 | 策略缓存5日过期 | ✅ | 保存→读取TTL=5.0天；人为过期→load返回None→触发重新消融 |
| 7 | 历史买卖点按所选策略 | ✅ | 保存"MACD·保守"→analyze后 sel_algo=macd，信号全部来自MACD金叉/死叉；保存"KDJ·稳健"→信号来自KDJ规则；回测止损参数同步使用策略参数 |
| 8 | 幽灵K线 | ✅ | slice_view 追加T+1/T+5/T+10三根（无重复）；离屏渲染主图/副图/指标/侧栏/报告全通过；悬停标签"预测T+5(累计)" |
| 9 | GUI全管线回归 | ✅ | 4类标的 analyze+slice+渲染（主板/创业板/ETF/题材）；平移视图；侧栏策略行；报告策略行 |
| 10 | CLI 端到端 | ✅ | `python stock_predict.py sh600004` 完整输出（进度百分比、样本、信号、回测）；含消融引擎与策略缓存 |
| 11 | 荐股深库内存安全 | ✅ | 改为分块+窗口函数取尾部400根后，全库扫描15s完成（原整库载入在500万行级别会占2GB+内存） |
| 12 | 数据清洗扫描 | ✅ | 74万根日K全库扫描22s：结构异常0、越界814只（多为北交所旧不复权数据）、停牌缺口87只、疑似退市99只 |
| 13 | numpy回退正确性 | ✅ | numpy路径与纯Python路径距离最大差 <1e-12（同一批窗口逐项对比） |
| 14 | 限流自愈 | ✅ | 501触发全局暂停120s→5min递增，域名轮换；实测暂停后自动恢复抓取 |

## 三、关键设计说明

### 3.1 消融防过拟合（为什么这么算）
- 候选在**训练集**上选出：保守=最小回撤、稳健=最大收益/回撤比、激进=最大年化（各要求≥3笔交易）；
- **验证集绝不参与选型**——弹窗同时给出两段指标，用户能看到"训练好看、验证拉胯"的过拟合gap并自行降档；
- 信号生成零前视：形态匹配只用 T 日及以前窗口、指标用截至 T 日序列、信号日收盘成交、止损用其后日内 low/high。

### 3.2 幽灵K线口径
- T+1 由预测K线（P50开合/区间高低）承担；T+5/T+10 取多日预测累计 P50 收、P75 高、P25 低（含均值回归衰减），开=前一预测日收盘；高/低做了 open/close 包夹修正；
- 策略决定**买卖点与回测口径**；幽灵K线始终由三级融合统计预测驱动（指标型策略没有天然的逐日价格路径，不伪造）。

### 3.3 数据源与配额现实（诚实边界）
- 东财 push2his 批量抓取约 500 只后对该IP断连（整域4主机一致）；腾讯单域名突发配额约100-200请求，之后501；
- 因此回填采用：**2×800翻页（腾讯）三域名轮换 + 501全局暂停120s→300s + 断点续传**——单晚约能推进数百只，连续运行数晚可覆盖全市场；
- 已验证代理出口（127.0.0.1:7890）可叠加配额，脚本自动读取 ini 代理配置。

## 四、发现并修复的问题（本次改造连带）

1. **`_bar_ok` 影线规则误杀老数据**：原"影线>实体1.5倍剔除"在低价股老K线上误杀率30%+（实测某股第二页800根被杀252根），且该规则本身无统计依据。已改为仅结构性校验（缺失/非正/高低颠倒），价格异常由涨跌幅校验兜底。
2. **`load_pools_progressive` 重复累计**：每批样本 `acc_rows +=` 执行两次，样本翻倍污染匹配。已修复。
3. **`daily_picks` 整库载入**：全市场×1000根后会占2GB+内存。已改为"根数初筛→分块→窗口函数取尾部400根"。
4. **东财增量拉取`count=900`**：与1000根目标不匹配，缓存永远凑不齐深历史。已把增量拉取上限提到 1100（`get_daily` 内）。

## 五、遗留与后续

- [ ] 全市场1000根覆盖需连续运行 `python backfill_full.py` 数晚（断点续传自动跳过已达标代码；东财恢复后单次请求即可拉全量，效率翻倍）；
- [ ] 北交所（bj）代码源端K线支持仍缺失（腾讯主域501期间未验证），清洗脚本对 bj 只删坏bar/登记退市不重拉；
- [ ] L2/L3 池匹配同样可向量化（当前向量化了 d_px 主距离，环境惩罚项仍逐候选计算，60股池实测已 <2s）；
- [ ] 消融的 L1 形态档步长固定5日，后续可做成自适应。

## 六、回归测试方法

```bash
python -m py_compile stock_gui.py stock_predict.py data_clean.py backfill_full.py
python backfill_full.py --limit 50        # 回填冒烟
python data_clean.py                      # 清洗扫描（只读）
python stock_predict.py 002241            # CLI 端到端
python stock_gui.py                       # GUI：分析→弹窗选策略→幽灵K线→工具重选策略
```

*本报告所有输出均为历史数据统计与研究用途，不构成投资建议。*
